server:
  port: 8080

spring:
  application:
    name: sports-lottery-system

  datasource:
    url: jdbc:mysql://localhost:3306/lottery_system?useUnicode=true&characterEncoding=UTF-8&serverTimezone=Asia/Shanghai&useSSL=false&useSSL=false
    username: root
    password: wjz19940823
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
        format_sql: true

  jackson:
    date-format: yyyy-MM-dd HH:mm:ss
    time-zone: Asia/Shanghai

logging:
  level:
    com.lottery: INFO
    org.hibernate.SQL: WARN
